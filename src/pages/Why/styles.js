import styled from "styled-components";
import { Options, WhatMain } from "../What/styles";

export const WhyMain = styled(WhatMain)``;
export const NewOptions = styled(Options)`
    flex-direction: column;
    align-items: unset;
`;
